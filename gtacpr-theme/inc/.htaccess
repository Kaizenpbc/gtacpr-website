# Block direct HTTP access to all files in this directory.
# These files are PHP includes only — they should never be requested by a browser.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
